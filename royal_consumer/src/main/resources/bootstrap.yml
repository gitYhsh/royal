spring:
  application:
    name: nocos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.8.187:8848 #注册中心地址
    sentinel:
      transport:
        port: 8179
        dashboard: 192.168.8.187:8080   # sentinel配置
server:
  port: 17358
feign:
  sentinel:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"